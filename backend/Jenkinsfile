pipeline {
    agent any
    
    tools {
        nodejs 'Node_22_13'
        'hudson.plugins.sonar.SonarRunnerInstallation' 'SonarScanner'
    }
    
    stages {
        stage('Install and Test') {
            steps {
                dir('backend') {
                    sh 'npm install'
                    sh 'npm run test:cov -- --maxWorkers=1'
                }
            }
        }

        stage('SonarQube Analysis') {
            steps {
                dir('backend') {
                    script {
                        def snHome = tool name: 'SonarScanner', type: 'hudson.plugins.sonar.SonarRunnerInstallation'
                        
                        sh "sed -i 's|SF:.*src/|SF:src/|g' coverage/lcov.info"

                        withSonarQubeEnv('SonarServer') { 
                        sh "${snHome}/bin/sonar-scanner -Dsonar.javascript.node.maxspace=512 -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info -Dsonar.scm.disabled=true"
                        }
                    }
                }
            }
        }

        stage("Quality Gate") {
            steps {
                dir('backend') {
                    timeout(time: 8, unit: 'MINUTES') {
                        waitForQualityGate abortPipeline: true
                    }
                }
            }
        }
    }
}